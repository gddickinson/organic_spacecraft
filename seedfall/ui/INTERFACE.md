# `seedfall/ui/` — map

PyQt6 presentation. Holds no rules; sim acts log themselves.

Generated by `python -m seedfall.tests.maps --write` from each module's own docstring (the first sentence of it); the `maps` suite fails when a module is missing here. Why the pieces are shaped as they are is in [`../INTERFACE.md`](../INTERFACE.md) and [`../notes/`](../notes/README.md).

162 modules.

## The window

- `app.py` — Application entry point: build the Qt app, show the title screen, run.
- `window.py` — The main window: status bar, navigation rail, the stack of screens, and the ship's log.
- `window_dialogs.py` — The main window's modal surfaces — the toast, the panel, the question.
- `view_registry.py` — Which class draws which screen, and the one place that builds them all.
- `menubar.py` — The menu bar: everything the window can do, in one place you can find.
- `hud.py` — The heading bar: the stardate, the purse, the meters, and the despatches.
- `log_panel.py` — The ship's log down the right of the window: built once, then appended to.
- `crash.py` — What happens when something goes wrong inside the running window.
- `popout.py` — One door for opening a pop-out window, and for letting it go.
- `monitors.py` — Pop-out instrument windows you can leave open beside the game.
- `title.py` — The title screen: the premise, a seed box, and the way back in.
- `chronicle_picker.py` — The chronicles on this machine, as the title screen offers them, and the menu's "Save as…".
- `seed_dialog.py` — Choosing what to plant on a body, and what it will be worth.
- `beginning_view.py` — Choosing who you are, with every choice stating what it will do.
- `tutorial_bar.py` — A strip along the top saying what to try next.
- `focus.py` — Keeping the player's place on the keyboard across a rebuild.
- `flow.py` — Buttons that wrap onto as many lines as the width needs.
- `view_base.py` — The base class every screen subclasses, and the row that wraps.
- `widgets.py` — Reusable pieces of the interface: labels, panels, bars, cards and tabs.
- `theme.py` — The GESTALT visual identity, as Qt palette and stylesheet.
- `painting.py` — One door for painting on a widget, and for surviving not being able to.
- `layer_row.py` — One hull layer as a row: its name, what kind of layer it is, and how much of it is left.
- `endings.py` — How a chronicle ends, and the offer to carry on past it.

## The chart and the system

- `map_view.py` — The sector chart screen: the chart, what you know about the star you have picked, and the ways to fly there.
- `star_chart.py` — The star chart itself: forty-odd stars, the jump circle, the Weave, and the growing red stain in one corner of it.
- `reaches_chart.py` — The Far Reaches on the sector chart: one frame per region, and the gates.
- `sky_chart.py` — The sky on the Sector Chart: a ring and a glyph per phenomenon.
- `system_view.py` — Inside a system: the bodies, what a survey found on them, and the four things you can do about it — look, dig, dive, or plant a seed.
- `survey_panel.py` — The four ways of looking at a body, each saying what it cannot see.
- `crossing_panel.py` — Choosing how hard to fly a crossing, and what each way costs on both clocks.
- `anchorage_panel.py` — Where you can put in, and how to get back to it.
- `traffic_panel.py` — Who else is out here, and what they are doing.
- `life_panel.py` — The life catalogue, grouped by what it runs on.
- `mesh_panel.py` — What the picket mesh is hearing, from systems you are not in.
- `orbit_chart.py` — The orrery: the system drawn as a chart you can click.
- `reaches_panel.py` — A deep anchor, where you are standing at one: what relighting it takes.
- `sky_strip.py` — The sky, on the screens: the System screen's Sky strip and the shelter mark.
- `weave_panel.py` — The Weave, as the sector chart shows it.
- `spheres.py` — A world drawn as a lit disc rather than as a bag of polygons.
- `surface.py` — Painting a world's features onto its disc.

## Flying

- `helm_view.py` — The helm — plotting a transfer inside a system.
- `pilot_view.py` — The Pilot's screen: what is out there, all the time, with the clock running.
- `pilot_panels.py` — The bridge's two columns, and the boards that go in the right-hand one.
- `pilot_acts.py` — What the bridge's own controls do when they are pressed.
- `conn_window.py` — The conn: a pop-out window for flying the ship by hand.
- `conn_controls.py` — The conn's console: everything the pilot can press, and what it promises.
- `conn_panel.py` — The conn's side panel: what it says, and updating it without rebuilding it.
- `conn_moves.py` — The acts that swap the conn's flight for another one.
- `conn_targets.py` — Which contact the conn should open on.
- `flight_window.py` — The flight controls, in a window a pilot can actually berth from.
- `flight_clock.py` — The one clock that flies `game.conn`, and the one beat it delivers.
- `flying_keys.py` — Keys for the six axes, on every screen a hand can fly from.
- `autopilot_bar.py` — The autopilot, reachable from every screen a hand can fly from.
- `approach_window.py` — Ship and target together, from outside, with where they are going.
- `approach_plot.py` — The docking approach, drawn: where the collar is and where you are.
- `viewport.py` — What a camera on the hull sees.
- `viewport_hud.py` — Heads-up flying aids, drawn on the picture the pilot already looks through.
- `viewport_mark.py` — The ring on whatever the course is laid on, drawn out of the window.
- `viewport_math.py` — Where a direction lands on the screen, and nothing about painting.
- `viewport_target.py` — The thing being approached, as a camera sees it: the solid, its bracket, and the arm coming out to take the ship.
- `sights.py` — Which contacts get a name in a window, and the bearing to each.
- `mount_sight.py` — A sight down one mount: its arc, and where the target sits in it.
- `thrust_pad.py` — The thrust pad: six thrusters, the drive switch and the coast — one widget.
- `gauges.py` — Instruments, drawn: dials, segmented meters, traces and a scope.
- `plot_canvas.py` — The plotting board's canvas: a system seen obliquely, with tracks on it.
- `plot3d_window.py` — The plotting board: a pop-out system plot you can navigate from.
- `render3d.py` — A small solid-3D renderer: meshes, a light, and something worth looking at.
- `stars3d.py` — What a star looks like out of a window, class by class.
- `thumb3d.py` — A small rendered portrait, for the catalogue to actually show something.
- `effects.py` — How long a thing that happened stays on the glass, and how hard it lands.
- `effect_paint.py` — Painting what just happened, on the picture the pilot is already looking at.
- `effect_marks.py` — The marks a contact leaves on a picture: the flash, what flies off, the cracks.
- `effect_clock.py` — The short clock that runs an animation after the flight's own clock has stopped.

## Fighting

- `battle_view.py` — Combat.
- `battle_orders.py` — The battle's orders: every act open this turn, and what each will do.
- `battle_text.py` — The battle screen's words: forecasts and aftermath, as sentences.
- `battle3d.py` — The engagement from the bridge: two hulls, and what passed between them.
- `tactical_plot.py` — The engagement from above: hulls, headings, range bands and firing arcs.
- `tactical_board.py` — The readiness board: what a fight would look like, in figures.
- `tactical_window.py` — The tactical station, open whether or not anybody is shooting.
- `gunner_window.py` — The gunner's station, in its own window.
- `fire_panel.py` — The fire control: what may be fired on, at what range, and the act itself.
- `firing_panel.py` — What each mount can do this turn, and what would fix the ones that cannot.
- `doctrine_panel.py` — What the battle computer intends at the seats you are not sitting in.
- `assessment_panel.py` — The read on an engagement: who is winning, why, and what to do about it.
- `hunts_panel.py` — The Hunts tab on the Law screen: who has a grudge, what the board pays, and a search whose odds you read before you spend a day on it.
- `hunt_marks.py` — The rivalry's small marks on other people's screens.
- `gunnery_view.py` — The gunnery school, and the way into any gun on any hull you command.
- `turret_window.py` — The gun, in a window you can actually fight from: the beat, the keys, the hands.
- `turret_view.py` — The gunner's window: the sky, the glass over it, and the boards beside it.
- `turret_scene.py` — The sky out of a gun blister: your own hull, what is out there, and the tracers.
- `turret_hud.py` — The glass in front of a gunner: the bore, the pip, the brackets, the gauges.
- `turret_panels.py` — The gunner's three boards: what is locked, what is around, what is wanted.
- `turret_controls.py` — The gunner's console: everything the hands can reach, and what it promises.

## Port, trade and the yard

- `port_view.py` — A port: the market, the services counter, and whoever is looking for a berth.
- `market_grid.py` — The Port's price board, kept between refreshes and changed in place.
- `board_panel.py` — The contract board, drawn: what is posted, and what it takes to do it.
- `commissions_panel.py` — The commissions desk: work that leads somewhere, and where you are in it.
- `freight_panel.py` — The freight desk: what is worth loading here, and where it goes.
- `register_panel.py` — The register: what you wrote down at other ports, and how old it is.
- `blackmarket_panel.py` — The unposted market: what they will pay here for what they forbid here.
- `berths_panel.py` — The berths tab: who is on your bridge, how they feel, and who is hiring.
- `official_panel.py` — The person behind the counter, and what can be asked of them.
- `rumours_panel.py` — Word going round, and the survey business.
- `yard_view.py` — The shipyard: design a hull, refit the one you have, and manage the fleet.
- `shipdiagram.py` — The ship itself, small, with its engines lit when they fire.
- `machineshop.py` — The machine shop: where a captain actually gets a robot, and sends it away.
- `robots_panel.py` — The machines you own, and what each is worth where it is standing.
- `concourse_panel.py` — The Port screen's Concourse tab: what is here, and the way into it.

## The ship

- `ship_view.py` — The ship: the layer stack, what is fitted, who is aboard, and the hold.
- `plans_panel.py` — The ship, drawn.
- `body_panel.py` — The Body tab: what the hull has been through, and what it is becoming.
- `arc_panel.py` — The officers' stories on the screens: the Ship screen's Crew tab, a beat's answers on the Despatches board, and the Codex's crew page.
- `dormancy_panel.py` — Putting the crew under: what it saves, what it costs, who might not wake.
- `mining_panel.py` — The workings: what is in a body, how deep, and how you mean to get it.

## The crew

- `crew_view.py` — The Crew screen: the muster book, one person at a time, and every watch.
- `crew_roster.py` — The Roster tab: the complement, the watch bill, and a card for every hand.
- `crew_sheet.py` — The Sheet tab: one person, whole.
- `crew_ops.py` — The Watches tab and the book: what a crew costs, what mends it, who has gone.

## The concourse

- `concourse_view.py` — The Concourse screen: everywhere people are, and everything they sell.
- `concourse_shops.py` — The Shops tab: the purse, the counters, the shelf, and the paperwork.
- `concourse_body.py` — The Clinic tab: what this place will do to a person, and to whom.
- `concourse_night.py` — Board and bed, and the evening: what a crew does when the watch turns.
- `concourse_law.py` — The 'Who runs this' tab: the noticeboard at the gate.

## Holdings, the house and the Voyage

- `empire_view.py` — Holdings — colonies, the depot, and how close each ending is.
- `works_panel.py` — The development desk for one colony: what it has built, and what it could.
- `industry_panel.py` — Selling a process: who would pay for it, and what it does to their quays.
- `exchequer_panel.py` — The public purse, from the outside: what each power earns and is building.
- `house_panel.py` — The trading house: its account, its lines, its masters and its books.
- `house_dialog.py` — The new-line dialog: pick a hauler, a master, the ports and the rules, and see what it will clear before you commit.
- `voyage_panel.py` — The Voyage: the rank and the next, the ten ending tracks as ladders, the milestones lately reached, and the perks a rank carries.
- `counsel_card.py` — The first officer's counsel, as a card on the Sector Chart.
- `renown_chip.py` — The renown chip: the moment a milestone is reached.
- `programmes_panel.py` — Standing programmes: what the bench runs once it has learned everything, and what becomes of what it finds.
- `ventures_panel.py` — What the powers are doing, and whether you mean to help.

## The powers and the law

- `diplomacy_view.py` — The diplomacy desk — standing, the relations matrix, and overtures.
- `assembly_panel.py` — The Assembly tab on the Diplomacy screen: the next sitting, the order paper, what each power will vote and why, what moving a vote would cost, what is in force, and what …
- `envoy_view.py` — A power's envoy, waiting on an answer.
- `demand_view.py` — Somebody has put your holding inside their border and wants an answer.
- `law_view.py` — The docket: who has what on you, what it will cost, and the way out.

## Research, the ground and the Kith

- `tech_view.py` — The research tree — ten branches, five tiers, and one project at a time.
- `tech_tree.py` — The research tree's cards, kept between refreshes and changed in place.
- `inquiry_panel.py` — The bench: what evidence you hold, what the programme wants, and how hard you mean to push it.
- `xeno_view.py` — The xenology desk — what the four cultures left, and how far you understand it.
- `expedition_view.py` — The ground.
- `dig_view.py` — The trench: which stratum you are on, and how you mean to take it.
- `minigame_view.py` — The two mini-game screens: the docking loop and the decoding bench.
- `kith_panel.py` — A Kith gathering: the lexicon, the gift, and what may be asked.
- `kith_codex.py` — The Kith on other people's screens: the Codex page and the chart's mark.

## News, help and the rest

- `despatch_view.py` — Despatches and the chronicle: the sector's mail, and the ship's own record.
- `comms_window.py` — The comms window: talk to a thing, and see what can be done with it.
- `codex_view.py` — The codex — the fleet class reference, the powers, and the vocabulary.
- `help_view.py` — Help and options: how the game works, and the handful of things you can set.
- `academy_panel.py` — The Academy: the whole curriculum, and a way into any part of it.
- `legacy_view.py` — The epoch you are living in, the pressure on it, and what wants an answer.
- `memoir_panel.py` — The memoir on the Aftermath screen, and the Hall of Captains on the title.
- `orders_panel.py` — Standing orders: what the ship could usefully be doing next.
- `transit_view.py` — The pilot's seat during a crossing: the watch, and what it wants.
- `options_view.py` — The options page: one panel, shown either in a window or inside Help.

## Sound

- `audio.py` — The one door to the speaker: `play`, `loop`, `set_level`, `volume`.
- `synth.py` — Every sound in the game, made from nothing on first run, and kept.
- `soundmap.py` — What the game sounds like: game events to cues.
