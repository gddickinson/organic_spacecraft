"""Engine primitives: seeded RNG, formatting helpers, the save codec, and the
game state that owns the clock. No game rules, no Qt.

Contents (13 modules; one line each in `INTERFACE.md`):

Modules: clock, guard, ids, llm, loading, rng, save, sectortime, shiptime,
    slots, solid, state, util."""
