"""Engine primitives: seeded RNG, formatting helpers, the save codec, and the
game state that owns the clock. No game rules, no Qt.

Contents (14 modules; one line each in `INTERFACE.md`):

Modules: cli, clock, guard, ids, llm, loading, rng, save, sectortime,
    shiptime, slots, solid, state, util."""
